import { Endpoint } from "./endpoint";

export class Test{

    id: number;
    endpoint: Endpoint;
    name: string;
    numberOfRequests: number;
    databaseTime: number;
    apiTime: number;
    applicationTime: number;

}